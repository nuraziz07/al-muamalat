import React from 'react';
import CourseCard from '../../../../../components/Cards/CourseCard'

const Courses = () => {
    return (
        <div>
            {/*<CourseCard course={} />*/}
        </div>
    );
};

export default Courses;